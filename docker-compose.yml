services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      AUTH_SECRET: ${AUTH_SECRET}
      DATABASE_URL: file:/app/data/dev.db
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
